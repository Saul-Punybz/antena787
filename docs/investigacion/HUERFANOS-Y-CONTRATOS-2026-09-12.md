# Huérfanos y contratos — 12 sept 2026

Formato: `archivo:línea → qué → a propósito o descuido`.

1. `internal/api/presets.go:1-205` + `internal/store/catalog.go:480-600` (PresetRepo) + `internal/store/migrate.go:271-286` (tabla `preset`) → 5 rutas completas y correctas (`/presets` GET/POST/PUT/DELETE + `/material/{id}/volver-a-preparar`), con cliente en `web/src/lib/api.ts:156-166` y tipos en `web/src/lib/tipos.ts:835-874` → **descuido**. ~450 líneas del 11 sept sin una sola pantalla que las llame: `grep` de `presets()/crearPreset/guardarPreset/borrarPreset/volverAPreparar/AjustesDePreset` en `web/src/pantallas` y `web/src/componentes` da cero resultados. A diferencia de `sage`/`hdhomerun`, ningún comentario propio dice qué tanda lo cablea, y no aparece en `docs/f2/PLAN-F2.md` ni en `docs/ACEPTACION.md`.
2. `internal/api/contrato_test.go` (1200 líneas) → no menciona `Preset`, `PresetsDelCanal` ni `AjustesDePreset` en ningún caso de prueba → **descuido**, gemelo del punto 1: el huérfano tampoco tiene quien lo pruebe.
3. `internal/store/schema.sql:39-46` (tabla `capture_input`) → 0 SELECT/INSERT/UPDATE en todo `internal/`; solo comentarios (`internal/app/asistente.go:76`, `internal/drivers/captura/hdhomerun/hdhomerun.go:1`) → **a propósito**: `docs/auditoria/01-codigo-muerto.md` (11 sept) ya lo marcó para la tanda T8, y `docs/ACEPTACION.md` F2-81/82 no llevan "Construido en".
4. `internal/store/schema.sql` (tablas `manual_hold`, `air_recording`, `overlay`) → 0 código real en todo el repo → **a propósito, con aviso al cliente**: `docs/INSTALAR-WINDOWS.md` dice "el botón [Tomar el control] está pero todavía no hace nada", "No graba lo que salió al aire", "No pone el logo". El botón sí existe (`web/src/pantallas/AlAire.tsx:300-325`) pero no llama nada.
   - **`manual_hold`: cerrado el 12 sept 2026** (tanda T5). `internal/store/manual.go`, `internal/app/manual.go`, `internal/api/manual.go` y el panel de `AlAire.tsx`; pruebas en `internal/app/manual_test.go`. El aviso salió de las dos guías de instalación el mismo día.
   - `air_recording` y `overlay` siguen esperando, y las guías lo siguen diciendo.
5. `internal/store/schema.sql` (`advertiser`, `insertion_order`, `corte`, `break_marker`, `spot_airing`, `classified`, `classified_airing`, `portal_link`, `payment`) → 0 uso salvo `advertiser` (una lectura menor) → **a propósito**: el propio esquema lo rotula "tablas desde F1 para que el esquema no cambie; se usan en F4".
6. `web/src/lib/tipos.ts:983` (`TarjetaDeRed`) ↔ `internal/api/salidas.go:43` ↔ `internal/drivers/salida/udpts.go:36` (`Interfaz`) ↔ `internal/engine/encoder.go:74` (`LocalAddr`) → **revisado, sin problema**: el contrato de la tarjeta de red agregada hoy está cableado completo, de la pantalla `Salidas.tsx` hasta el comando de ffmpeg.
7. `PARA-ROLANDO.md` y `docs/INSTALAR-WINDOWS.md` → **revisado, sin problema**: no prometen nada que el código no alcance; ambos avisan explícitamente lo que falta (Anuncios, grabación de aire, logo). El control manual estaba en esa lista y se quitó de las dos guías el 12 de septiembre, cuando dejó de faltar.
